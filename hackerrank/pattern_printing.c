/** https://www.hackerrank.com/contests/pattern/challenges/displaypattern/problem */

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int row = 0, col = 0, max_row = 0, max_col = 0, n = 0;
    printf("\nEnter a number:");
    scanf("%d", &n);
    if (n <= 0 || n > 10)
      {
        exit(EXIT_FAILURE);
      }

   start_row = 0;
   start_col = 0;
   max_row = n;
   max_col = n;

   for (row = start_row ; row < max_row; )
      {
        for (col = start_col; col < max_col; )
           {
              
           }
      }


   for (row = start_row ; row < max_row; )
      {
        for (col = start_col; col < max_col; )
           {

           }
      }
       
    return 0;
}

